<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.organizze">

    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizze">
        <activity
            android:name="ativity.EditProfileActivity"
            android:exported="false"
            android:parentActivityName="ativity.MainScreenActivity"/>
        <activity
            android:name="ativity.ReceitasActivity"
            android:exported="false"
            android:theme="@style/receitasTheme" />
        <activity
            android:name="ativity.DespesasActivity"
            android:exported="false"
            android:theme="@style/despesasTheme" />
        <activity
            android:name="ativity.MainScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/Theme.Organizze.NoActionBar" />
        <activity
            android:name="ativity.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="ativity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>